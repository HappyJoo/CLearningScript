#include <stdio.h>

int main() {
    int a, *p = &a;
here:   char *q = reinterpret_cast<char*>(&a);
    //char *q = (char *)&a;
    printf("%p %p\n", p, q);
    printf("%p %p\n", p+1, q+1);
    return 0;
}
